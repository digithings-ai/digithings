# Copy to local.env (gitignored): cp config/local.env.example config/local.env
# Default URI when Supabase CLI local stack is running (`supabase start`).
# Safe to commit — local dev credentials only.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
