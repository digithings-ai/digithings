/**
 * Deprecated re-export: ConvictionMeter now lives in @digithings/web
 * (components/conviction, promoted verbatim). Import from the package in
 * new code; this shim keeps the existing dashboard importers compiling.
 */
export { ConvictionMeter } from "@digithings/web";
