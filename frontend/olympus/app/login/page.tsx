'use client';

import { LoginScreen } from '@/components/login-screen';

/** Static-export login page — PKCE OAuth starts here (no route handlers). */
export default function LoginPage() {
  return <LoginScreen />;
}
