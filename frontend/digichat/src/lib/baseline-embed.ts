/**
 * Unconfigured container / no-host `/embed` chrome. Not a tenant brand and
 * not catalog `base`. YAML that omits `skin` still parses as `base`.
 */

export const BASELINE_EMBED_SKIN = "digichat" as const;
export const BASELINE_EMBED_WELCOME = "Ask a question";
export const BASELINE_EMBED_WELCOME_BODY = ["Ask about anything you need help with."];
export const BASELINE_EMBED_PLACEHOLDER = "Ask a question";
