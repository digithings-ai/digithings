# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Local knowledge base (e.g. Obsidian vault; not part of the app)
knowledge/
*.swp
*.swo

# Temp files
*.tmp
*.bak

# Secrets (never commit these)
.env
*.key
secrets/
config/supabase.env
config/local.env
config/mcp.secrets.env
frontend/.env
frontend/.env.local

# Next.js build cache
.next/
out/

# Tooling caches
.vite/

# Node (if any scripts use it)
node_modules/

# Python
__pycache__/
*.pyc
.venv/

# Local data tree: price CSV cache, agent scratch, migrated markdown — not in git (Supabase is canonical; see RUNBOOK.md + data/README.md)
data/*
!data/README.md

# Deprecated repo path (never commit — canonical state is Supabase)
outputs/

# Dashboard data JSON (generated by update_tearsheet.py — not committed)
frontend/public/dashboard-data.json
