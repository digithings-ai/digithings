import { SystemToPipelineRedirectPage } from '@/components/legacy-spa-redirect';

/** Legacy System surface — run health moved to Pipeline; bookmarks land there. */
export default SystemToPipelineRedirectPage;
