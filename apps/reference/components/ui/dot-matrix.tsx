"use client";

/**
 * Gallery DotMatrix is the promoted first-party cube matrix.
 * Source of truth: @digithings/ui/chat/dot-matrix (never the main barrel).
 */
export {
  DotMatrix,
  dotMatrixStates,
  litCellsFor,
  type DotMatrixProps,
  type DotMatrixState,
} from "@digithings/ui/chat/dot-matrix";
