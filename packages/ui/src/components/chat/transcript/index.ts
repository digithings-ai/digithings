/**
 * Transcript serializers + activity rows for the package skins (WS4).
 *
 * Moved from `@digithings/digichat-ui` (which re-exports this barrel for
 * compat). The three modules share no export names, so `export *` is safe.
 */
export * from "./transcript-markdown";
export * from "./activity-view";
export * from "./types";
