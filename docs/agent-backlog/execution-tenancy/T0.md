<!-- title: [agent] T0 — Workspaces + tenant columns + RLS privacy boundary -->

Parent: epic "Olympus client-ready". Spec §5-T0 in
`docs/superpowers/specs/2026-08-29-kairos-tenancy-implementation-spec.md`; detailed schema source:
Wave 3 roadmap §P2 at `digiquant/src/digiquant/olympus/atlas/docs/ops/MIGRATION-ROADMAP-DIGITHINGS.md`.
**Wave A — can run in parallel with K0** (disjoint files).

## Primary component
digiquant · **Risk** high (data exposure) · **Execution tier** claude · **Model** sonnet ·
Review: RLS diff needs careful human/`/review` pass before merge.

## Read first
`CLAUDE.md`, `digiquant/AGENTS.md`, Wave 3 roadmap §A.4 (decision record) + §P2a–P2c,
`digiquant/supabase/SCHEMA.md` (current tables + RLS conventions), spec §2 privacy boundary.

## Branch
`make task ISSUE=<this>` → `task/<N>-workspaces-rls` off `module/digiquant`.

## Goal
Multi-tenant schema: `workspaces` (+ billing columns), `workspace_members`, `stripe_events`,
`job_runs`, `audit_log`; `workspace_id` on private tables; RLS policies drafted **behind a
cutover flag** — production anon behavior must not change until T1 ships login.

## Files — create/modify
- Create migration(s) `digiquant/supabase/migrations/<next-free>_workspaces_tenancy.sql`
  (split into 2–3 files if cleaner: tables / tenant columns / policies)
- Update `digiquant/supabase/SCHEMA.md` (new section: Tenancy)
- Create `digiquant/src/digiquant/olympus/tenancy.py` (typed `Workspace`, `PlanTier` StrEnum
  `free|baseline|custom|enterprise`, `SYSTEM_WORKSPACE_SLUG = "system"`, helpers to resolve the
  system workspace id) + `tests/dq/olympus/test_tenancy.py`
- Create `tests/dq/olympus/test_migration_tenancy.py` (structural)

## Binding behavior
1. Tables per roadmap P2a with spec deltas: `plan_tier` CHECK in
   (`free`,`baseline`,`custom`,`enterprise`); exactly one `type='system'` workspace (partial
   unique index); seed the system row + a `house` owner workspace idempotently.
2. `workspace_id uuid` added to the **private set**: `positions`, `position_events`,
   `nav_history`, `portfolio_metrics`, all `portfolio_ledger_*`, `olympus_accounting_*`,
   `broker_connections`/`broker_orders`/`broker_executions`/`broker_position_snapshots` (if K4/K3
   merged; else skip with a comment), `notification_prefs` (if K5 merged), overlay rows of
   `olympus_profile_config` (house row → system workspace). Backfill everything existing to the
   house workspace id. NULLable first, backfill, then `SET NOT NULL` — one migration, explicit steps.
3. Uniques widened per roadmap P2b (e.g. `positions UNIQUE(date,ticker)` →
   `UNIQUE(workspace_id,date,ticker)`) — enumerate every constraint you change in the migration
   header comment.
4. RLS: new policies for `authenticated` (own-workspace rows via `workspace_members`; system
   workspace research readable per tier — the tier CHECK itself lands in T5's policy pass, keep a
   `TODO(T5)` marker) — but **do not drop any existing `anon` policy in this WP**. Removal of
   anon `USING (true)` is a separate flagged migration shipped inside T1's release train (spec
   T0/T1 cutover note). State this loudly in the migration header.
5. Nothing in Python writers changes behavior: `service_role` bypasses RLS; writers keep working
   with explicit house workspace id defaults (add the column to insert payloads where the schema
   now requires it — grep every writer touching the private set: `commit_io`, `ledger_io`,
   `execution_io`, `opening_snapshot`, accounting writers, `execute_at_open` projections).

## Acceptance criteria
- [ ] Two-JWT RLS test plan documented in the migration header (executable proof lands with T1;
      here: pgTAP-style structural assertions that policies exist and anon policies are untouched)
- [ ] House pipeline regression: `pytest -m unit tests/dq/olympus/ -v` green (writers carry the
      new column; no behavior change)
- [ ] Structural migration test: tables, CHECKs, seeds idempotent (run twice in test), every
      widened UNIQUE listed
- [ ] SCHEMA.md updated; ruff clean; `make test-baseline` green; `make score` ≥ 8/8/7/9
- [ ] PR description contains the full list of tables gaining `workspace_id` + constraints changed

## Out of scope (do NOT do)
Dropping/narrowing anon policies (T1 train), Auth UI (T1), Stripe (T2 — but the billing columns
land here per roadmap), tier CHECK inside research policies (T5), BYOK table (K3/T3 own it),
touching corpus keys (they stay tenant-agnostic — never add workspace_id to
`olympus_research_corpus` keys).
