# Mini-plan: #2589 Seed holding_lots / remove --no-ledger

**Branch:** `task/2589-seed-portfolio-ledger-holding-lots-and-r` → `module/digiquant`

## Policy

1. **Cold start:** If open `holding_lots` are empty and the prior `positions` book has holdings → must not measure residuals (would EXIT/OPEN-mislabels).
2. **Seed:** One labeled `legacy_opening_snapshot` chain (`policy_version_id`) derived from the latest positions book + NAV + marks — not invented pre-cutover history. Idempotent.
3. **Auto-ensure** in `build_events_from_paper_fills` before `execute_pending_orders`.
4. **Cutover flags:** drop `--no-ledger` on both call sites; add `--require-ledger`; update cron pin tests + ARCHITECTURE.

## Out of scope

- Deleting prose builders
- Live trading
- WP3 accounting
