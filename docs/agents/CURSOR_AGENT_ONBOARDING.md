# Cursor Agent Onboarding — Tier 2 Delegation

This document covers everything needed to connect a Cursor Pro background agent to
the DigiThings issue board and have it autonomously execute `exec:cursor` tasks.

## Prerequisites

- Cursor Pro or Business subscription (Background Agents feature required)
- GitHub account with write access to `digithings-ai/digithings`
- `develop` branch checked out locally (Cursor will branch from it)

---

## 1. One-time setup in Cursor

### 1a. Connect GitHub

1. Open Cursor → **Settings** → **Integrations** → **GitHub**
2. Authenticate with the GitHub account that has write access to the repo
3. Confirm the `digithings-ai/digithings` repository appears in the list

### 1b. Enable Background Agents

1. Open Cursor → **Settings** → **Beta** → enable **Background Agents**
2. Cursor will ask for a compute budget — start with the default; adjust as usage grows

### 1c. Repo-level context

Cursor reads `.cursorrules` / `.cursor/rules/` automatically. The file
`.cursor/rules/digithings.mdc` (generated by `make agents-init`) contains:

- Non-negotiable rules (Polars, Pydantic v2, NautilusTrader, LiteLLM, scoring thresholds)
- Component routing table
- Execution tier definitions
- Subagent + skill prose summaries

No additional configuration is needed — the rules file is the context layer.

---

## 2. How Cursor picks up issues

### Label convention

Every issue eligible for Cursor carries the label **`exec:cursor`**.

The GitHub Actions workflow `.github/workflows/cursor-agent-dispatch.yml` fires
the moment that label is applied. It posts a structured comment on the issue with:

- A preflight checklist (component AGENTS.md path, test command, score threshold)
- The exact task description formatted for Cursor's context window
- A link to open the issue directly in Cursor Background Agents

### Triggering the agent

**Option A — Manual (recommended while warming up):**
1. Open the issue in GitHub
2. Click the "Open in Cursor" deep-link in the dispatch comment
3. Cursor opens with the task pre-loaded; click **Start Background Agent**

**Option B — From within Cursor:**
1. Open the Background Agents panel (`Cmd/Ctrl + Shift + A`)
2. Paste the issue URL or number
3. Cursor fetches the issue body and begins

**Option C — Fully automated (future):**  
When `CURSOR_API_KEY` is configured as a repo secret, the dispatch workflow will
call the Cursor API directly to enqueue the task without manual intervention. See
[§6 — Future automation](#6-future-automation) below.

---

## 3. What the agent must do

Cursor agents operating on this repo **must follow this sequence**:

### 3a. Pre-flight (before writing any code)

```
1. Read {component}/AGENTS.md            ← pre-flight checklist for that module
2. Read {component}/ARCHITECTURE.md      ← module map, extension points
3. Confirm the issue is exec:cursor:
   - Single component, <5 files estimated
   - Clear acceptance criteria (no ambiguity)
   - No cross-module integration required
   - No live-trading paths (digiquant/live/, config/live*)
   If any condition fails → comment on issue explaining why, relabel exec:claude, stop.
```

### 3b. Branch naming

```
git checkout -b cursor/<issue-number>-<slug>
# Example: cursor/142-add-unit-tests-digisearch-chunker
```

Branch from `develop` (or the relevant `module/<component>` branch if one is active).

### 3c. Implementation rules

The full rules are in `.cursor/rules/digithings.mdc`. Key constraints:

- **Polars only** — never import pandas
- **Pydantic v2** — model_validator, field_validator; no v1 syntax
- **No comments** unless the WHY is non-obvious
- **No error handling for impossible scenarios** — trust framework guarantees
- **Ruff clean** — `ruff check . && ruff format .` must pass
- **Tests required** — every change needs a unit test in `tests/unit/`
- **Never touch** `digikey/`, `config/litellm.yaml`, `docs/scoring/`, `SECURITY.md`

### 3d. Scoring gate

Before opening the PR, run the self-score:

```bash
make score          # must pass: Security ≥8, Quality ≥8, Optimization ≥7, Accuracy ≥9
make test-unit      # all unit tests green
ruff check .        # zero violations
```

If score fails: fix and re-score. If still failing after two attempts, open the PR
anyway but flag `score:below-threshold` in the PR body so a human reviews it.

### 3e. PR submission

```bash
git push origin cursor/<issue-number>-<slug>
# Open PR via GitHub UI or gh CLI
```

PR must:
- Target `develop` (or the active `module/<component>` branch)
- Body must contain `Closes #<issue-number>`
- Body must fill in the PR template checklist (scoring, test evidence, doc flag)
- Title format: `feat(<component>): <description> (#<issue-number>)`

**Do not merge the PR yourself.** A human (Claude Code Tier 3) reviews and merges.

---

## 4. Scope constraints — what Cursor must never do

| Constraint | Reason |
|---|---|
| No changes to `.github/workflows/` | Protected path; requires human gate |
| No changes to `docs/scoring/` | Scoring rubric is locked |
| No changes to `SECURITY.md` | Security doc is locked |
| No changes to `digikey/` auth | Auth changes require Tier 3 + security review |
| No changes to live-trading paths | Always human-gated |
| No cross-module changes in one PR | Increases blast radius; split into per-module issues |
| No `exec:claude` issues | Relabel and comment instead of attempting |
| No force-push | Branch history must be clean |

If any constraint is violated mid-task: open an `exec:claude` issue describing the
situation, close the cursor branch without pushing, and comment on the original issue.

---

## 5. Issue quality checklist (for the human filing exec:cursor issues)

For Cursor to succeed, the issue body must have:

- [ ] **Component** field filled in (from the issue template dropdown)
- [ ] **Goal** — one paragraph, no ambiguity
- [ ] **Acceptance criteria** — 3–5 testable bullet points
- [ ] **Docs expectation** — will ARCHITECTURE.md need updating? (yes/no)
- [ ] **Risk** set to `low` or `medium` (high → exec:claude)
- [ ] **No dependency on another open issue**

Use `/spec` in Claude Code to generate compliant issue bodies automatically.

---

## 6. Future automation

When Cursor exposes a stable REST API for background agent dispatch, the workflow
`.github/workflows/cursor-agent-dispatch.yml` will be updated to call it directly
using `CURSOR_API_KEY` stored as a GitHub repo secret.

To prepare:

```bash
# In GitHub repo → Settings → Secrets → Actions
# Add: CURSOR_API_KEY = <key from Cursor Settings → API>
```

The dispatch workflow already checks for this secret and will activate the API call
once it's present. Until then, it posts the manual deep-link comment.

---

## 7. Monitoring running agents

- **Cursor UI:** Background Agents panel shows active agents, logs, and PR links
- **GitHub:** PRs from `cursor/*` branches appear in the PR list; labelled `exec:cursor`
- **Issue board:** The dispatch comment is updated when the agent opens its PR
- **CI:** Standard PR quality gate + component tests run automatically on the branch

If an agent appears stuck (no PR after 30 min for a scoped task), cancel it in Cursor
and file an `exec:claude` issue with the error logs from the Background Agents panel.

---

## 8. Reference

| Resource | Path |
|---|---|
| Execution tiers | `docs/agents/EXECUTION_TIERS.md` |
| Component routing | `docs/agents/COMPONENT_ROUTING.md` |
| Agent workflow | `docs/agents/AGENT_WORKFLOW.md` |
| Cursor rules | `.cursor/rules/digithings.mdc` |
| Scoring rubrics | `docs/scoring/` |
| Dispatch workflow | `.github/workflows/cursor-agent-dispatch.yml` |
| Issue template | `.github/ISSUE_TEMPLATE/agent_task.yml` |
